module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  }
}


// NOTE
// POSTCSS: a tool for transforming CSS with JS plugins like:
// 1. Parsing CSS: transform extended syntaxes and future CSS features into a format that current broswer support
// 2. Optimization: Compress CSS by removing unnecessary white spaces, comments, etc.